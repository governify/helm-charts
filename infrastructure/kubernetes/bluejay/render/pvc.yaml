kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: bluejay-render
  namespace: governify-bluejay
  labels:
    app: bluejay-render
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
