kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: bluejay-join
  namespace: governify-bluejay
  labels:
    app: bluejay-join
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
