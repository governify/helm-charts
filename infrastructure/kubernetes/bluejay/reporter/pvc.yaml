kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: bluejay-reporter
  namespace: governify-bluejay
  labels:
    app: bluejay-reporter
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
