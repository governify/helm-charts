kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: bluejay-dashboard
  namespace: governify-bluejay
  labels:
    app: bluejay-dashboard
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
